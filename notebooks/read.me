All notebooks used in the paper

PLAIN_* files are versions corresponding to the PLAIN ensemble. 
Replace the original files (without the PLAIN_ prefix) with this in tabpfn-extensions/src/tabpfn_extensions/post_hoc_ensembles/
